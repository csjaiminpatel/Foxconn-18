import { SvDataFormatterPipe } from './sv-data-formatter.pipe';

describe('SvDataFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SvDataFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
